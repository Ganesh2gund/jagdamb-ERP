/// Fallback stub for mobile and desktop platforms where dart:html is unavailable
void openHtmlContent(String htmlContent) {
  // On mobile devices, PDF / native printing is used instead of browser window printing.
}
